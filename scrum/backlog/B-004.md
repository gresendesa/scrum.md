# B-004 - Build Python CLI shell and JSON contract

```yaml
section: backlog.item.B-004
id: B-004
title: Build Python CLI shell and JSON contract
status: todo
type: implementation
po_priority: 2
risk: medium
linked_sprint:
owner: Guilherme
created_at: 2026-06-09
updated_at: 2026-06-09
tags: [backlog, implementation, cli, python, typer, json, foundation, smd]
```

## Objective

```yaml
section: backlog.item.B-004.objective
title: Objective
status: active
owner: Guilherme
tags: [objective]
```

Create the executable Python CLI shell for `smd`, exposing the memory foundation through Typer commands and a stable JSON contract.

## Scope

```yaml
section: backlog.item.B-004.scope
title: Scope
status: active
owner: Guilherme
in_scope:
  - Add a Python package and `smd` entrypoint using Typer.
  - Implement global options `--json`, `--root`, `--quiet`, and `--verbose`.
  - Load `.smd/config.yml` with safe defaults.
  - Implement the JSON output envelope defined by the specification.
  - Add thin command handlers for foundation queries, backed by B-003 primitives.
out_of_scope:
  - Full backlog, sprint, gate, and context command behavior.
  - Template rendering internals beyond invoking B-002 primitives.
  - Git audit and protected-file diff enforcement.
tags: [scope, cli, json, config]
```

## Acceptance Criteria

```yaml
section: backlog.item.B-004.acceptance-criteria
title: Acceptance Criteria
status: active
owner: Guilherme
criteria:
  - `smd --help` exposes the CLI entrypoint and documented global options.
  - Foundation commands can emit the stable JSON envelope with `ok`, `command`, `timestamp`, `root`, `data`, `warnings`, and `errors`.
  - Errors return non-zero exit codes and structured error objects.
  - Configuration is loaded from `.smd/config.yml` when present.
  - Automated tests cover CLI invocation and JSON envelope determinism.
tags: [acceptance, cli, json, tests]
```

## Dependencies

```yaml
section: backlog.item.B-004.dependencies
title: Dependencies
status: active
owner: Guilherme
dependencies:
  - B-003
  - DEC-005
tags: [dependencies]
```

This item wraps the memory foundation from B-003 in a user-facing CLI shell.

[@ref: Memory foundation](B-003.md#backlog.item.B-003)
[@ref: Architecture decision](../decisions.md#decisions.DEC-005)

## History

```yaml
section: backlog.item.B-004.history
title: History
status: active
owner: Guilherme
events:
  - date: 2026-06-09
    event: Created from the technical specification after the PO approved Python and Typer for the implementation foundation.
  - date: 2026-06-09
    event: Refined to wrap the memory foundation after agent-operable queries exist.
  - date: 2026-06-09
    event: PO set priority to high and left the item outside SPR-2026-03.
tags: [history]
```

[@ref: Command specification](../../specification.md#specification.commands)
[@ref: JSON output contract](../../specification.md#specification.output-contract)
