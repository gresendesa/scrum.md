# Imperativos

1. Leia atentamente a especificação técnica da CLI `smd` no arquivo `specification.md` para entender a arquitetura, filosofia e fluxo de trabalho da ferramenta.
2. Implemente a CLI `smd` seguindo as diretrizes e comandos descritos na seção "Comandos CLI Disponíveis para a IA" da especificação.
3. Use como referência o template `templates/default/`
4. Se não existir, primeiro passo é criar nossa estrutura de memória scrum para este projeto (sem usar cli, porque ainda não está implementada), seguindo a arquitetura descrita na especificação.